import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mightymongoosewiggle',
  appName: 'mighty-mongoose-wiggle',
  webDir: 'dist'
};

export default config;
